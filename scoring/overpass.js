// scoring/overpass.js — OSM Overpass API client
// Step 6 will replace this stub with real batched queries + sessionStorage cache.

export async function fetchRoadData(points) {
  // Step 6 — wire up real Overpass API here
  return null;
}
